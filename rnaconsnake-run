#!/bin/bash
# Deprecated alias for ./RNAcs, kept so existing scripts keep working.
set -euo pipefail

repo_root="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
echo "rnaconsnake-run is deprecated; use ./RNAcs instead." >&2
exec "$repo_root/RNAcs" "$@"
